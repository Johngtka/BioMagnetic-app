import { PointNamesPipe } from './point-names.pipe';

describe('PointNamesPipe', () => {
    it('create an instance', () => {
        const pipe = new PointNamesPipe();
        expect(pipe).toBeTruthy();
    });
});
