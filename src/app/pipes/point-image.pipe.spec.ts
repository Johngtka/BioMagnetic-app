import { PointImagePipe } from './point-image.pipe';

describe('PointImagePipe', () => {
    it('create an instance', () => {
        const pipe = new PointImagePipe();
        expect(pipe).toBeTruthy();
    });
});
